<form method="post" action="{{ path('app_typesproperty_delete', {'id': typesproperty.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typesproperty.id) }}">
    <button class="btn">Delete</button>
</form>
