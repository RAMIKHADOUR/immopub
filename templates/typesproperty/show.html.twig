{% extends 'base.html.twig' %}

{% block title %}Typesproperty{% endblock %}

{% block body %}
    <h1>Typesproperty</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typesproperty.id }}</td>
            </tr>
            <tr>
                <th>Types_property</th>
                <td>{{ typesproperty.typesProperty }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ typesproperty.updatedAt ? typesproperty.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_typesproperty_index') }}">back to list</a>

    <a href="{{ path('app_typesproperty_edit', {'id': typesproperty.id}) }}">edit</a>

    {{ include('typesproperty/_delete_form.html.twig') }}
{% endblock %}
